# Claude Code - Exclude sensitive session data
history.jsonl
projects/
debug/
session-env/
shell-snapshots/
todos/
file-history/
plans/
paste-cache/
usage-data/
sessions/

# Exclude personal config with account info
.claude.json
.claude.json.backup.*

# Exclude ephemeral caches
stats-cache.json
statsig/
cache/
.claude/

# Only commit configuration
!settings.local.json

# Exclude marketplace config which has a timestamp that's being updated all the time
plugins/known_marketplaces.json
plugins/install-counts-cache.json
plugins/installed_plugins.json

# Exclude marketplace content (embedded git repos)
plugins/marketplaces/

# Stuff
ide
telemetry
tasks
policy-limits.json

.credentials.json
mcp-needs-auth-cache.json
